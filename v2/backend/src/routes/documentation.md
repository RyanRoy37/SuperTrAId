# Auth Routes Documentation

This folder contains Express route definitions for authentication:
- `/signup` – Register new user
- `/login` – Authenticate user and return JWT

Routes use controllers for logic and middlewares for validation/auth.
