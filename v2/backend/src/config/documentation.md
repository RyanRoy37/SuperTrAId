# Config Folder Documentation

This folder contains configuration files for:
- PostgreSQL connection
- MongoDB connection
- Environment variables

All credentials and sensitive data should be managed via `.env` and not hardcoded.
